#include <bits/stdc++.h>
using namespace std;
int n, k;
int f[20003];
int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t; cin >> t;
    while(t--){
        cin >> n >> k;
        memset(f, 0, sizeof(f));
        f[]
        for (int i = 1; i <= 2003; i++){
            for (int j = 0; j < n; j++){
                
            }
        }
    }
}
